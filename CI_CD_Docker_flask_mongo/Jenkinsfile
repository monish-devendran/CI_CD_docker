pipeline{
  agent any
  stages{
    stage("first-test")
    {
      steps{
        script {
            env.PATH = "$WORKSPACE"
        }
        echo "${env.PATH }"
      }
    }
    stage("deploy"){
        steps{
            sshagent(['docker-aws']) {
                // some block
                sh """
                
                
                """
            }
        }
    }
  }
}
